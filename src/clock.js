import React from 'react';
import ReactDOM from 'react-dom';

 function Tick() {

    return (
    // const element = (
      <div>
        <h1>Hello, world!</h1>
        <h2>It is {new Date().toLocaleTimeString()}.</h2>
      </div>
    // )
    // ReactDOM.render(element, document.getElementByClass('root'))
    )
  }
  
//   setInterval(tick, 1000)

export default  Tick;
